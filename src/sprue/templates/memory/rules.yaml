- name: claims-coverage
  command: ["python3", ".sprue/scripts/check-claims-coverage.py", "--quiet"]
  scope: whole

- name: images-valid
  command: ["python3", ".sprue/scripts/check-images.py", "--quiet"]
  scope: page

- name: asset-capture
  command: ["python3", ".sprue/scripts/check-asset-capture.py", "--quiet"]
  scope: whole

- name: triage-done
  command: ["python3", ".sprue/scripts/check-triage-done.py", "--quiet"]
  scope: page